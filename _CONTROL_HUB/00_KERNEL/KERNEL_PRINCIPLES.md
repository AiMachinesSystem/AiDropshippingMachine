---
machine: "eBay / AutoDS Dropshipping Machine"
type: kernel
status: v1 (extracted, not re-invented)
date: 2026-06-22
created_real: 2026-06-22
source: extracted from CLAUDE.md (constitution) — pointers only, no logic duplicated
---

# MACHINE KERNEL — governing rules (extracted)

> The minimal rule-set that governs the system. Source of truth = `CLAUDE.md`. This is a pointer/summary, not a fork.

## CORE PRINCIPLES
- **Mission:** research → validate → launch → manage → measure → improve → scale an eBay/AutoDS dropshipping business. Owner: Luca (IT commands).
- **Loop:** Collect → Structure → Analyze → Decide → Plan → Execute-internally → Measure → Learn → Improve.
- **Never invent data.** Every load-bearing claim carries an evidence label.

## EVIDENCE LABELS (constitutional set — do not extend without owner amendment)
`[OBSERVED — source+date]` · `[INFERRED — basis]` · `[UNKNOWN]` · `[PUBLIC RESEARCH REQUIRED]` · `[ESTIMATE declared]`. Proxy counts = lower bounds. Absences = LOW-SAMPLE flag.

## GO GATE (hard, per-action, NEVER transfers)
Internal (research, analysis, drafts, reports, plans, audit, safe reversible fixes) → proceed autonomously.
**GO-class (stop, cite the gate):** publishing/editing listings, prices, business policies; AutoDS automations; supplier orders/payments; account login/connect; any spend; external writes; credentials; deletions; moving/renaming main folders; git push.

## FIREWALL (CORE → DOMAIN → CATEGORY → INSTANCE, never upward)
This is an INSTANCE (eBay/AutoDS dropship). No cross-project transfer of data/claims/skills. Poker/trading/digital-products stay isolated. (Active flag: `n8n_digital_products/` + `shopify_*` residue = R4, owner decision.)

## DATA-QUALITY GATES (learned, enforced)
- Destructive actions verified by independent read before "done" (E-005).
- Market numbers only from CANONE CORRENTE of the registry; freshest registered run wins on conflict.
- A research/profit run is INCOMPLETE without registry + cockpit refresh.

## LEARNING LOOP
Every error → ERROR_REGISTRY (ERRORE→CAUSA→REGOLA→TEST). A process fix is true only if it passes in a separate cold process. Patterns become skills only with a near, real use (anti-overbuilding §9).
