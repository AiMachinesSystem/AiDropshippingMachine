---
description: Run the ads-library-scan skill on a page, brand, keyword, or niche
---
Use the **ads-library-scan** skill on this target: $ARGUMENTS

Follow the skill exactly: mode selection first (query vs run), the r.jina.ai
proxy method with retry ≤2 and known walls marked [UNKNOWN], lower-bound
counts stated, page-level relevance verification, and the mandatory
RESEARCH_MEMORY_INDEX registration at close-out. v1.1 discipline: canonical
numbers from the latest registered run only, universe separation in counts,
Library-ID collision protocol, QUERY MODE answer template (answer + file +
run date).
