---
description: Run the launch-prep skill to build a gated launch pack for a validated niche
---
Use the **launch-prep** skill on: $ARGUMENTS

Follow the skill exactly: inputs check first (registered niche block +
owner decisions), GATE ZERO on product provenance/rights (no answer →
skeleton + sourcing options + decision request, then STOP), declared-basis
anchors only, real guarantee only, explicit GO gates on every live action,
RDAP checks read-only (never register), index registration at close-out.
