---
description: Run the competitor-scan skill on one or more competitors
---
Use the **competitor-scan** skill on this target: $ARGUMENTS

Follow the skill exactly: mode selection first (query vs run), STEP 0 niche
context resolution via RESEARCH_MEMORY_INDEX, depth tiering declared before
extracting, pre-declared 1–5 scoring rubric, evidence labels everywhere, and
the mandatory index registration at close-out. v1.1 discipline: canonical
numbers from the latest registered run only, universe separation in census
deltas, QUERY MODE answer template (answer + file + run date).
