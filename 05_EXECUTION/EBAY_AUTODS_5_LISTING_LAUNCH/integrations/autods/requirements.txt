# autods_readonly_test.py — Python standard library ONLY (Python 3.8+; tested 3.12).
# No third-party packages required (uses urllib, json, os, sys from the stdlib).
# Nothing to `pip install`. If you later build a richer client, pin deps here.
