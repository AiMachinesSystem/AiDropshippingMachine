# TEMPLATE (tracked in git — contains NO secrets). Copy to `autods_credentials.env` and fill locally.
# The real `autods_credentials.env` is gitignored and must never be committed or pasted in chat.
AUTODS_EMAIL=
AUTODS_PASSWORD=
