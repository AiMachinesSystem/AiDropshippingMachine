# --- OS / editor ---
.DS_Store
Thumbs.db
*.log
.obsidian/workspace*.json

# --- dipendenze locali ---
node_modules/

# --- scratch effimero (script usa-e-getta, NON versionato; tieni solo il README) ---
/_tmp/*
!/_tmp/README.md

# --- 90_CACHE: NON versionata (si mantiene solo lo scheletro via .gitkeep) ---
/90_CACHE/fetches/**
/90_CACHE/screenshots/**
/90_CACHE/tools/**
/90_CACHE/selftests/**
/90_CACHE/*.py
/90_CACHE/sims/**

# riammetti i segnaposto di struttura
!**/.gitkeep

# --- scaffolding di import (NON versionato; rimuovibile dopo verifica owner) ---
/_IMPORT/
/ebay_autods_dropshipping_machine.zip

# --- SECRETS: MAI versionati (real .env, chiavi, token, credenziali). .env.example resta tracciato ---
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
secrets.json
credentials.json
*.secret
*.token

# Python virtual environments / local runtimes
.venv/
**/.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.playwright-mcp/
storage_state_ebay.json
**/storage_state_ebay.json
